spring:
  application:
    name: api-gateway
  cloud:
    config:
      url: http://configserver:8888
    consul:
      host: consul-server
      port: 8500
      enabled: true
      discovery:
        register: false

encrypt:
  failOnError: false


