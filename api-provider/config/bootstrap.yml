spring:
  cloud:
    consul:
      host: consul-server
      port: 8500
      enabled: true
      discovery:
        register: true
        serviceName: api-provider
        instanceId: id-${random.value}